// we can add comma(,) after last list element it doesn't affect collection
void main(){
    List<String> strList;
    strList = [
        'google',
        'microsoft',
        'Apple',
        'Netflix',
    ];
    print(strList);
}